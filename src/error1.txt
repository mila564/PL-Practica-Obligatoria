funcion entero max (entero a, entero b)
inicio
    bifurcacio (a > b)
        inicio
            return a;
        fin
    sino
        inicio
            return b;
        fin
fin

funcion entero Principal()
inicio
        caracter aux;
        aux = "Esta es una funcion principal";
        mostrar(aux);
fin

procedimiento Principal()
inicio
    caracter aux;
    aux = "Este es un procedimiento principal";
    mostrar(aux);
fin