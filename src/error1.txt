funcion entero max (entero a, entero b)
inicio
    bifurcacio (a > b)
        inicio
            return a;
        fin
    sino
        inicio
            return b;
        fin
fin

funcion entero Principal()
inicio
        caracter aux;
        aux = 'f'
        mostrar(aux);
fin

procedimiento Principal()
inicio
    caracter aux;
    aux = 'p';
    mostrar(aux);
fin